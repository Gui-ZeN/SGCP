node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Claude Code (local)
.claude/

# Dumps de leitura do Firestore de produção (scratch de diagnóstico).
# Não são fonte, e mesmo sem nome de candidato são dados reais do cliente.
vagas.json

# Cache local do detector do impeccable (o config.json fica versionado).
.impeccable/hook.cache.json
